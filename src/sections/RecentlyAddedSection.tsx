import { SectionHeader } from "../components"

export const RecentlyAddedSection = () => {
    return (
        <section>
            <SectionHeader title="Recently Added:" />
            <p className="text-gray-500">Currently Empty.</p>
        </section>
    )
}